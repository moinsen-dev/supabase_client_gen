<!-- GENERATED by supabase_client_gen — edit the contract, not this file -->

# wave1_contract v0.1.0

`2026-06-12` · project `wave1` · ref `wave1ref00000000`

## Tables

### sessions

| Field | Type | Null | Notes |
|---|---|---|---|
| id | uuid |  | PK |
| code | text |  |  |
| created_at | timestamptz |  |  |

`select: authenticated · insert: edge_function_only · update: edge_function_only · delete: edge_function_only`

### songs

| Field | Type | Null | Notes |
|---|---|---|---|
| id | uuid |  | PK |
| session_id | uuid |  |  |
| title | text |  |  |
| duration | integer |  |  |
| thumbnail_url | text | ✓ |  |

`select: authenticated · insert: authenticated · update: authenticated · delete: authenticated`

### session_players

| Field | Type | Null | Notes |
|---|---|---|---|
| session_id | uuid |  | PK |
| user_id | uuid |  | PK |
| role | text |  |  |
| joined_at | timestamptz |  |  |

`select: authenticated · insert: authenticated · update: authenticated · delete: authenticated`

### session_queue_overview `view`

| Field | Type | Null | Notes |
|---|---|---|---|
| id | uuid |  | PK |
| session_id | uuid |  |  |
| title | text |  |  |
| vote_count | integer |  |  |

`select: authenticated · insert: authenticated · update: authenticated · delete: authenticated`

## RPC Functions

`add_song_to_player_queue(p_session_id: uuid, p_user_id: uuid, p_title: text, p_duration: integer, p_thumbnail_url?: text) → uuid`
Adds a song to the calling player's personal queue.

`get_vote_tally(session_uuid: uuid, song_uuid: uuid) → json`

`get_session_by_code(p_code: text) → row:sessions`
Fetch a session row by join code.

`list_session_songs(p_session_id: uuid, p_since?: timestamptz) → rows:songs`
All songs in a session, in queue order.

`reset_session(p_session_id: uuid) → void`
Clears all queues in a session.

`heartbeat() → boolean`
Liveness probe with no arguments.

`queue_length(p_session_id: uuid) → integer`

## Realtime

Publication: `public.songs`, `public.session_players`

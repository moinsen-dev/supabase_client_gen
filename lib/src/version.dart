/// The package version, kept in sync with pubspec.yaml. Surfaced via the
/// `--version` flag on both CLIs so a generated client's provenance can be
/// traced back to the exact generator release.
library;

const String packageVersion = '0.4.0';

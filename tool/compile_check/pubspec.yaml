name: compile_check
description: Throwaway package that compile-checks generated client code against real Supabase/Equatable deps. Not published.
publish_to: none
version: 0.0.0
environment:
  sdk: ^3.3.0
  flutter: ">=3.0.0"
dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.8.0
  equatable: ^2.0.0
